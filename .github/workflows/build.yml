name: Build installers

# Builds the Windows .exe and macOS .dmg in the cloud.
# Run it manually from the Actions tab ("Run workflow"), push to main, or push a
# version tag like v1.0.0 to also attach the installers to a GitHub Release.
on:
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      # Call the tools directly via node (avoids pnpm's pre-run dependency check).
      - name: Type-check
        run: node node_modules/typescript/bin/tsc -b

      - name: Build web bundle
        run: node node_modules/vite/bin/vite.js build

      - name: Package Windows installer
        if: matrix.os == 'windows-latest'
        run: node node_modules/electron-builder/out/cli/cli.js --win --publish never

      - name: Package macOS installer
        if: matrix.os == 'macos-latest'
        # No Apple signing certificate, so disable auto-signing (app still runs;
        # Mac users right-click > Open the first time, like Windows SmartScreen).
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false
        run: node node_modules/electron-builder/out/cli/cli.js --mac --publish never

      - name: Upload installers as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: evensen-hub-${{ matrix.os }}
          path: |
            release/*.exe
            release/*.dmg
          if-no-files-found: ignore

      - name: Attach installers to GitHub Release (on version tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release/*.exe
            release/*.dmg
