name: Build installers

# Builds the Windows .exe and macOS .dmg in the cloud.
# Run it manually from the Actions tab ("Run workflow"), or push a version
# tag like v1.0.0 to also attach the installers to a GitHub Release.
on:
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Type-check
        run: pnpm exec tsc -b

      - name: Build web bundle
        run: pnpm exec vite build

      - name: Package Windows installer
        if: matrix.os == 'windows-latest'
        run: pnpm exec electron-builder --win --publish never

      - name: Package macOS installer
        if: matrix.os == 'macos-latest'
        # No Apple signing certificate, so disable auto-signing (app still runs;
        # Mac users right-click > Open the first time, like Windows SmartScreen).
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false
        run: pnpm exec electron-builder --mac --publish never

      - name: Upload installers as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: evensen-hub-${{ matrix.os }}
          path: |
            release/*.exe
            release/*.dmg
          if-no-files-found: ignore

      - name: Attach installers to GitHub Release (on version tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release/*.exe
            release/*.dmg
